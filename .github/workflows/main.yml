name: premake-android-studio-ci
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Build example
      run: |
        cd example
        ../premake5 android-studio
        cd build
        gradle build --stacktrace
